pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        // Xposed API 官方仓库
        maven { url 'https://api.xposed.info/' }
    }
}
rootProject.name = "oplus-recents-fix"
include ':app'
